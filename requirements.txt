Flask==2.3.3
beautifulsoup4==4.12.2
requests==2.31.0
python-dotenv==1.0.0
lxml==4.9.3
# Dépendances optionnelles pour améliorer les performances
# gunicorn==21.2.0  # Pour la production
# redis==4.6.0      # Pour le cache (si nécessaire)
# celery==5.3.1     # Pour les tâches asynchrones (si nécessaire)
